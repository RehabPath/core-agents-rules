/**
 * core-llm-rules
 * 
 * Shared AI assistant rules for Cursor and Claude Code.
 * 
 * Rules are automatically generated on install via postinstall hook.
 * To manually regenerate rules, run:
 * 
 *   npx generate-llm-rules
 * 
 * Or add to your package.json scripts:
 * 
 *   "scripts": {
 *     "generate:rules": "generate-llm-rules"
 *   }
 */

export const version = '1.0.0'
export const name = 'core-llm-rules'
